name=isl
version=0.15
build() {
cd $name-$version
./configure  \
   --prefix=/tools  \
   --build=${CLFS_HOST} \
   --host=${CLFS_TARGET} \
   --disable-static   \
   --with-gmp-prefix=/tools
make
make install
}
