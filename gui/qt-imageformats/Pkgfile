# Depends on: qt5-base libwebp libmng jasper

description="A cross-platform application and UI framework, QtImageFormat files"
url="http://qt-project.org/"
packager="pierre at nutyx dot org"

name=qt5-imageformats 
version=5.8.0-rc
release=1

_name="${name/5-/}-opensource-src-${version}"

source=(https://download.qt.io/development_releases/qt/5.8/$version/submodules/${_name}.tar.xz)

build() {
cd ${_name}
qmake-qt5
make
make INSTALL_ROOT=$PKG install
}
