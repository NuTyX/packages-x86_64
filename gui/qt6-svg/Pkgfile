description='Classes for displaying the content of SVG files'
url='https://www.qt.io/'

packager="tnut <tnut@nutyx.org>"

name=qt6-svg
version=6.3.1
makedepends=(cmake ninja qt6-base)

source=(https://download.qt.io/official_releases/qt/${version:0:3}/$version/submodules/qtsvg-everywhere-src-$version.tar.xz)

build() {
 cmake -S qtsvg-everywhere-src-$version -B build -G Ninja
 cmake --build build
 DESTDIR=$PKG cmake --install build
}
devel() {
  cd $PKG
  bsdtar -cf \
  $PKGMK_PACKAGE_DIR/$name.devel${PKGMK_BUILDVER}any.cards.tar \
  usr/include \
  usr/lib/qt6/mkspecs \
  usr/lib/cmake/
  
  rm -r usr/include usr/lib/qt6/mkspecs \
  usr/lib/cmake
}
