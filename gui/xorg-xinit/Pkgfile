# Depends on: xorg-server xorg-xauth xorg-xrdb xorg-xmodmap

description="The xinit package contains a usable script to start the xserver."
url="https://xorg.freedesktop.org/"
packager="tnut <tnut@nutyx.org>"
contributors="Greg"

_name=xinit
run=(xorg-xauth xorg-xrdb xorg-xmodmap)

name=xorg-xinit
version=1.3.4
release=3

source=(http://ftp.x.org/pub/individual//app/${_name}-$version.tar.bz2)

build() {

	cd ${_name}-$version
	./configure --prefix=/usr --sysconfdir=/etc --localstatedir=/var \
	--with-xorg-xinitdir=/etc/X11/app-defaults
	
	make
	make DESTDIR=$PKG install
	
}
