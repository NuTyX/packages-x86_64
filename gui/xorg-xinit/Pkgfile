# Depends on: xorg-server desktop-file-utils shared-mime-info xorg-xauth xorg-xrdb xorg-xmodmap

description="The xinit package contains a usable script to start the xserver."
url="https://xorg.freedesktop.org/"
packager="greg <greg@nutyx.org>"
contributors="tnut"

_name=xinit
run=(desktop-file-utils shared-mime-info xorg-xauth xorg-xrdb xorg-xmodmap)

name=xorg-xinit
version=1.3.4
release=2

source=(http://ftp.x.org/pub/individual//app/${_name}-$version.tar.bz2)

build() {

	cd ${_name}-$version
	./configure --prefix=/usr --sysconfdir=/etc --localstatedir=/var \
	--with-xorg-xinitdir=/etc/X11/app-defaults
	
	make
	make DESTDIR=$PKG install
	
}
