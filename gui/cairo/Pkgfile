description="Cairo is a 2D graphics library with support for multiple output devices."
url="https://www.cairographics.org/"

packager="tnut <tnut@nutyx.org>"
contributors="Pierre B, Rems"

makedepends=(meson ninja lzo libpng
             glib xorg-pixman xorg-fontconfig xcb-util
             xorg-libxrender xorg-libxxf86vm)

name=cairo
version=1.17.4

source=(https://gitlab.freedesktop.org/cairo/cairo/-/archive/$version/$name-$version.tar.bz2)

build=meson

uptodate() {
local url ext

url="https://www.cairographics.org/releases/"
ext=".tar.xz"
lynx -read_timeout=20 -dump -listonly -nonumbers \
$url|grep $name-[0-9]|grep $ext$|sed "s@$url$name-@@"|sed "s@$ext@@"|tail -1
}
