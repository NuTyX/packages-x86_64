. lib/lsb/init-functions
if [ ! -f etc/proftpd.conf ]; then
      cp etc/vsftpd.conf{.example,}
      log_success_msg "${WARNING} /etc/vsftpd.conf created ${NORMAL}"
      log_warning_msg "${WARNING} Double check /etc/vsftpd.conf and adapted to your needs ${NORMAL}"
fi

