# Depends on: python freetype dbus-glib intltool poppler-glib ffmpegthumbnailer libgsf

description="A D-Bus thumbnailing service based on the thumbnail management D-Bus specification.
url="http://www.ohloh.net/p/tumbler-xfce"
packager="tnut <tnut@nutyx.org>"
contributors="Tyrry"

name=tumbler
version=0.2.0
release=1

source=(http://archive.xfce.org/src/xfce/$name/${version%.*}/$name-$version.tar.bz2)

build() {
cd $name-$version
./configure --prefix=/usr \
--sysconfdir=/etc
make
make DESTDIR=$PKG install
}
