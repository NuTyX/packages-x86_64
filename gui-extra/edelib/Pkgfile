# Description: Component construction library for Equinox Desktop Environment
# URL: http://equinox-project.org/
# Packager: Chris Farrell, timcowchip at gmail dot com
# Maintainer: Chris Farrell, timcowchip at gmail dot com
# Depends on: subversion fltk1 dbus-python doxygen libnotify libstdc++5 xorg-libxext xorg-libxpm ftjam

name=edelib
version=2.1
release=1

source=()


build() {
	svn co http://svn.code.sf.net/p/ede/code/trunk/edelib
    cd "$name"
    ./autogen.sh
	FLTK_CONFIG="/usr/bin/fltk-config"
   ./configure --prefix=/usr 
	
   	jam || true
    
    install -d "$PKG"/usr/{bin,etc,share}
    
	jam DESTDIR="$PKG" install || true
}
