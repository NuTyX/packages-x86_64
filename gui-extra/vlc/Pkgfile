# Description: Media player, streamer, and encoder. It can play from many inputs
# URL:  http://www.slackware.com/~alien/slackbuilds/vlc/
# Packager: pierre at nutyx dot org
# Depends on: libvlc qt-base x264 flac libmad glib librsvg alsa-lib xorg-libxinerama xorg-libxpm libvpx libcdio vcdimager live libdvdcss libdvdread libdvdnav libogg libtheora lame faac libvorbis ffmpeg faad2 liba52 libmpeg2 libmpcdec fribidi lua libdv speex 

run=(libvlc)
name=vlc
version=3.0.0
release=20160606

source=( http://anduin.linuxfromscratch.org/BLFS/$name/$name-$version-$release.tar.xz)
	
build() {

source setqt

cd $name-$version-$release

sed -i '/seems to be moved/s/^/#/' autotools/ltmain.sh

BUILDCC=gcc \
./configure --prefix=/usr 


make
make DESTDIR=$PKG docdir=/usr/share/doc/$name-$version-release install

rm -r $PKG/usr/{include,lib/pkgconfig}
rm $PKG/usr/lib/libvlc*
}
