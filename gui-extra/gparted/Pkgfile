# Depends on: ssh-askpass gtk2 parted gtkmm2 libsigc++ ntfs-3g lvm2 docbook-xsl jfsutils btrfs-progs reiserfsprogs xfsprogs polkit mtools intltool perl-xml-parser

description="Gparted is the Gnome Partition Editor, a Gtk 2 GUI that can create, reorganise or delete disk partitions."
url="http://gparted.sourceforge.net/"
packager="tnut <tnut@nutyx.org>"
contributors="Pierre B, Tnut, Greg"

name=gparted
version=0.33.0
release=2

run=(polkit)

source=(https://downloads.sourceforge.net/$name/$name-$version.tar.gz)

build() {

cd $name-$version
./configure --prefix=/usr    \
  --disable-static \
  --enable-online-resize \
  --enable-libparted-dmraid \
  --enable-xhost-root --disable-doc
            
make
make DESTDIR=$PKG install

install -D -m0644 org.gnome.gparted.policy \
                  "$PKG"/usr/share/polkit-1/actions/org.gnome.gparted.policy
}
