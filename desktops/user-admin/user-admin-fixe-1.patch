diff -Naur a/src/main.c b/src/main.c
--- a/src/main.c	2019-09-19 11:41:08.000000000 +0200
+++ b/src/main.c	2019-11-29 13:20:32.686637482 +0100
@@ -150,17 +150,17 @@
     GtkWidget *Hbox1;
     GtkWidget *Hbox2;
 
-    Hbox = gtk_box_new(GTK_ORIENTATION_HORIZONTAL, 10);  
-    gtk_box_pack_start(GTK_BOX(Vbox),Hbox,FALSE,FALSE,0); 
+    Hbox = gtk_box_new(GTK_ORIENTATION_HORIZONTAL, 10);
+    gtk_box_pack_start(GTK_BOX(Vbox),Hbox,FALSE,FALSE,0);
     
-    Hbox2 = gtk_box_new(GTK_ORIENTATION_VERTICAL, 10);  
-    gtk_box_pack_start(GTK_BOX(Hbox),Hbox2,TRUE,TRUE,10); 
+    Hbox2 = gtk_box_new(GTK_ORIENTATION_VERTICAL, 10);
+    gtk_box_pack_start(GTK_BOX(Hbox),Hbox2,FALSE,FALSE,10);
     gtk_widget_set_size_request (Hbox2, 200,-1);
 
     /*Display user list on the left side*/   
     DisplayUserList(Hbox2,ua);
 
-    Hbox1 = gtk_box_new(GTK_ORIENTATION_VERTICAL, 10);  
+    Hbox1 = gtk_box_new(GTK_ORIENTATION_VERTICAL, 10);
     gtk_box_pack_start(GTK_BOX(Hbox),Hbox1,TRUE,TRUE,10); 
    
     /* Display the user's head image and name */
diff -Naur a/src/user-base.c b/src/user-base.c
--- a/src/user-base.c	2019-09-19 11:41:08.000000000 +0200
+++ b/src/user-base.c	2019-11-29 13:19:46.523304359 +0100
@@ -267,12 +267,11 @@
     LabelAutoLogin = gtk_label_new(NULL);
     SetLableFontType(LabelAutoLogin,"gray",11,_("Automatic logon"));
     gtk_grid_attach(GTK_GRID(table) , LabelAutoLogin , 0 , 3 , 1 , 1);
-    
     SwitchLogin = gtk_switch_new();
     ua->SwitchAutoLogin = SwitchLogin;
     gtk_switch_set_state (GTK_SWITCH(SwitchLogin),
                           GetUserType(user->ActUser));
-    gtk_grid_attach(GTK_GRID(table) , SwitchLogin , 1 , 3 , 1 , 1);
+    gtk_grid_attach(GTK_GRID(table) , SwitchLogin , 1 , 3 , 2 , 1);
     g_signal_connect(G_OBJECT(SwitchLogin),
                     "state-set",
                      G_CALLBACK(SwitchState),
