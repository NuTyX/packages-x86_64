packager="Schrodinger72 <schrodinger@nutyx.org>"
contributors="tnut"

makedepends=(vala git autoconf-archive libgee gtk3)

set=(gnome)

name=libgnome-games-support
version=1.8.0
release=1

source=(https://download.gnome.org/sources/$name/${version%.*}/$name-$version.tar.xz)

build=meson
